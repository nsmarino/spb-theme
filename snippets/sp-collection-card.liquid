<a href="{{ cardUrl }}" class="index-card">
  <div class="card-wrapper">

    <div class="image-wrapper">
      <img src="{{ cardImage | image_url }}" alt="{{ title }}">
    </div>

      <p>{{ title }}</p>
  </div>
</a>